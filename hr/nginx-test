apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: mmtest
  namespace: default
spec:
  interval: 1m
  chart:
    spec:
      chart: "/test-apps"
      version: '0.1.0'
      sourceRef:
        kind: GitRepository
        name: mmtest
        namespace: flux-system
      interval: 1m
  values:
    replicaCount: 2
