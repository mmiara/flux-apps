---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: rancher
  namespace: default
spec:
  chart:
    spec:
      chart: ./chart
#      version: '0.1.0'
      sourceRef:
        kind: GitRepository
        name: rancher
        namespace: flux-system
  interval: 60s
